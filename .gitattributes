# Auto detect text files and perform LF normalization
* text=auto

# These files are always text and should be normalized (LF line endings)
*.py text
*.txt text
*.md text
*.ps1 text
*.bat text

# These files are binary and should not be modified
*.ico binary
*.pem binary
*.key binary
*.crt binary